using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetCurrentSceneIntegerCaveBossRoom1 : MonoBehaviour
{
    public int currentSceneInteger = 3;

    // Start is called before the first frame update
    void Start()
    {
        currentSceneInteger = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
